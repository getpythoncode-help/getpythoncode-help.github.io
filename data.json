[
  {
    "Java week 7 Question 1 code": "     // Method to find the longest word in a given text\n    public static String findLongestWord(String text) {\n        String longestWord_IS = \"\";\n        \n        // Split the text into words based on whitespace\n        String[] words = text.split(\"\\\\s+\");\n        \n        // Iterate through each word to find the longest one\n        for (String word : words) {\n            if (word.length()  >  longestWord_IS.length()) {\n                longestWord_IS = word;\n            }\n        }\n        \n        return longestWord_IS;\n    }\n\n",
    "Java week 7 Question 2 code": "         // program to remove all occurrences of an element from array in Java.\n        public static int[] removeAll(int[] array, int elementToRemove) {\n               int[] result = new int[array.length];\n               int indexxx = 0;\n\n               // Iterate through the original array\n               for (int value : array) {\n                   // Copy only if the current element is not equal to the element to be removed\n                   if (value != elementToRemove) {\n                       result[indexxx++] = value;\n                   }\n               }\n\n               // If the resulting array is smaller than the original, resize it\n               return Arrays.copyOf(result, indexxx);\n        }\n",
    "Java week 7 Question 3 code": "//Code to create function primesum(), compute the sum of all prime numbers in a given range.\npublic static int primeSum(int x, int y) {\n    int sum = 0;\n    for (int iyi = x; iyi <= y; iyi++) {\n        if (isPrime(iyi)) {\n            sum += iyi;\n        }\n    }\n    return sum;\n}\n\nprivate static boolean isPrime(int num) {\n    if (num < 2) return false;\n    for (int i = 2; i * i <= num; i++) {\n        if (num % i == 0) return false;\n    }\n    return true;\n}\n ",
    "Java week 7 Question 4 code": "private int start;\nprivate int end;\n\npublic PrintNumbers(int start, int end) {\n    this.start = start;\n    this.end = end;\n}\n\n@Override\npublic void run() {\n    for (int lolo = start; lolo <= end; lolo += 2) {\n        System.out.println(Thread.currentThread().getName() + \": \" + lolo);\n    }\n}\n ",
    "Java week 7 Question 5 code": "public boolean isValidPassword(String password) {\n    // Step 1: Check if the password length is at least 8 characters\n    if (password.length() < 8) {\n        return false;\n    }\n\n    // Flag to track if there is an uppercase letter\n    boolean hasUpperCase7 = false;\n    // Flag to track if there is a number\n    boolean hasDigit = false;\n\n    // Step 2: Loop through each character in the password\n    for (char ch : password.toCharArray()) {\n        if (Character.isUpperCase(ch)) {\n            hasUpperCase7 = true; // Found an uppercase letter\n        }\n        if (Character.isDigit(ch)) {\n            hasDigit = true; // Found a number\n        }\n\n        // If both conditions are met, no need to check further\n        if (hasUpperCase7 && hasDigit) {\n            return true;\n        }\n    }\n\n    // Step 3: If either condition is not met, return false\n    return false;\n}\n "

  },
  {
    "Java week 4 Question 1 code": "         //Help me to get help \n \n        System.out.print(\"Roll Number is: \"+s.rollNo); ",
    "Java week 4 Question 2 code": "       //Want support ? Support me \n\n      System.out.print(\"Speed is: \"+c.speed); ",
    "Java week 4 Question 3 code": "          public int getBalance() {\n            return balance;\n        }\n    ",
    "Java week 4 Question 4 code": "         //Welcome to my world\n\n        m.salary = s;\n\n        // Print the salary\n        System.out.print(\"Salary is: \"+m.salary);",
    "Java week 4 Question 5 code": "       // Enjoyee\n\n        private int add(int a, int b, int c) {\n            return a + b + c;\n        }\n\n "
  },
  {
    "Java week 5 Question 1 code": " //Just simple method ok \n\nclass A implements Number {\n    public int findSqr(int i) {\n        return (i * i);\n    }\n}  ",
    "Java week 5 Question 2 code": " class B implements GCD {\n    int n1,n2;\n        \n    //#Create a method to calculate GCD take my help \n  \n    public int findGCD(int n1, int n2){\n        if(n1 < 0 || n2 < 0) {\n            return -1;   \n        }\n        if(n1==0 && n2==0) {\n            return 0;    // both zero\n        }\n        else if(n2 == 0){\n            return n1;   // gcd(n1,0) = n1\n        }\n        else {\n          return findGCD(n2, n1 % n2); } // recursive step\n        \n    }\n}  ",
    "Java week 5 Question 3 code": "  try {\n            result = a / b;\n            System.out.print(result);\n        }\n        // # catch block to catch the ArithmeticException\n        //Follow this only \n        catch (ArithmeticException e) {\n          System.out.print(\"Exception caught: Division by zero.\"); }\n         ",
    "Java week 5 Question 4 code": " //Just needed to add this try \n\ntry {\n          // traverse the array to find the total sum of the array\n          for (int i = 0; i < length; i++) {\n              name[i] = sc.nextInt();\n              sum += name[i];\n          }\n          System.out.print(sum);\n      }\n      catch(InputMismatchException e) {\n          System.out.print(\"You entered bad data.\");\n      }\n  ",
    "Java week 5 Question 5 code": "           try {\n              switch (i) {\n                  case 0 : \n                      int zero = 0; \n                      j = 92 / zero; \t\t\n                      break;\n                  case 1: \n                      //This int b[ ] will be blank only ok \n                      int b[ ] = null; \n                      j = b[0]; \t\n                      break;\n                  default:\n                      System.out.print(\"No exception\");\n              }\n          } \n          catch (Exception g) {\n            System.out.print(g);}\n          \n  "
  }

]

